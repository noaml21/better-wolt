JWT_SECRET=replace-with-a-long-random-secret

# Optional. Docker Compose reads this file from the repository root.
# When running the API without Docker (cd web-server && npm start), copy the
# values you need into web-server/.env instead.
# PORT=8080
# MONGODB_URI=mongodb://127.0.0.1:27017/better_wolt
# CORS_ORIGINS=http://localhost:3000,http://localhost:8080,http://localhost:8081,http://localhost:19006
